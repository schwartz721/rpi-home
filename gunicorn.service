[Unit]
Description=gunicorn serves the Flask app
After=network.target
StartLimitIntervalSec=0

[Service]
Type=simple
Restart=always
RestartSec=1
User=pi
ExecStart=gunicorn server:app

[Install]
WantedBy=multi-user.target